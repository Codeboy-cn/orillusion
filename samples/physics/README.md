# Legacy Ammo.js physics samples

These samples target the legacy `@orillusion/physics` (ammo.js) package.
For new projects use the Rapier-based samples in
[`samples/physics-rapier/`](../physics-rapier/) with
`@orillusion/physics-rapier` instead.
